#1. Your code goes here. Define a function called 'sign'
def sign(x):
    """
    This is the function to numerical argument and returns -1 if it's negative, 1 if it's positive, and 0 if it's 0.
    """
    if x>0:
        return 1
    elif x<0:
        return -1
    else:
        return 0
    
    ＃3. def is_negative(number):
    if number < 0:
        return True
    else:
        return False

def concise_is_negative(number):
    return number < 0 
# Your code goes here (try to keep it to one line!)

#5
def wants_all_toppings(ketchup, mustard, onion):
    """Return whether the customer wants "the works" (all 3 toppings)
    """
    return ketchup and mustard and onion
#
def wants_plain_hotdog(ketchup, mustard, onion):
    """Return whether the customer wants a plain hot dog with no toppings.
    """
    return not (ketchup or mustard or onion)
    